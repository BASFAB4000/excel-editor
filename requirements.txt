# Minimale Abhängigkeiten – werden automatisch über setup.py installiert.
# Nach dem Clonen: pip install -e .
# Oder: pip install -r requirements.txt && pip install -e .
openpyxl>=3.1
pandas>=2.0
pydantic>=2.0
pyinstaller>=6.0